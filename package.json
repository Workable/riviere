{
  "name": "riviere",
  "version": "0.1.3",
  "description": "",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "./node_modules/.bin/nyc node_modules/ava/cli.js test/**/*.js",
    "posttest": "./node_modules/.bin/nyc report --reporter=html --reporter=lcov",
    "coveralls": "cat ./coverage/lcov.info | node node_modules/.bin/coveralls",
    "eslint-check": "./node_modules/.bin/eslint --print-config .eslintrc.js | eslint-config-prettier-check",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.js": [
      "prettier --print-width 120 --single-quote true --write",
      "git add"
    ]
  },
  "dependencies": {
    "flat": "^2.0.1",
    "flatstr": "^1.0.5",
    "lodash": "^4.17.4",
    "log4js_honeybadger_appender": "^1.0.0",
    "uuid": "^3.1.0"
  },
  "devDependencies": {
    "ava": "^0.21.0",
    "coveralls": "^2.13.1",
    "koa": "^1.1.2",
    "koa-bodyparser": "^2.5.0",
    "nyc": "^11.1.0"
  },
  "engines": {
    "node": "6.10.x"
  }
}
